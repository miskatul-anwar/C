/*this is a program to calculate the area of a square*/
#include<stdio.h>
main()
{
    float a, area;
    printf("a=?");
    scanf("%f",&a);
    area=a*a;
    printf("area=%f",area);
    return 0;
}
