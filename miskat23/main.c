#include<stdio.h>
#include<math.h>
int main()
{
    float a=sqrt(pow(3,2)+pow(2,2)), b=sqrt((2*2)+(2*2)+(4*4));
    float A, x;
    A= (3*2) + (2*4);
    x=(A)/(a*b);
    printf("cosine(x)=%f",x);
    printf("\n");
    return 0;
}
