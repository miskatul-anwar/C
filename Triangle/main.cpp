/*A program to calculate the area of a triangle*/
#include<stdio.h>
main()
{
    float length, area;
    printf("length=?");
    scanf("%f",&length);
    area=0.433*length*length;
    printf("area=%f",area);
    return 0;

}
