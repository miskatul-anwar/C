#include<stdio.h>
#include<stdlib.h>
#include "basiccal.h"
int main(void){
    int x=9,y=10;
    hello;
    bar;
    printf("%f\n",pi);
    printf("%d\n",sum(x,y));
    printf("%d\n",sub(x,y));
    printf("%d\n",mul(x,y));
    printf("%d\n",div(x,y));
    bar;
    loop(2,10,2);
    char *s = malloc(N*sizeof(char*));
    char *sys_var = malloc(N*sizeof(char*));
    sys_var="name";
    entry(sys_var);
    get_string(s,N);
    printf("%s",s);
}